defmodule BeerNapkin.ExampleModelSpec do
  use ESpec.Phoenix, model: BeerNapkin.Example

  #let :example, do: %BeerNapkin.Example{title: "Example 1"}
  #it do: example.title |> should(eq "Example 1")
end
